# BROWN-HILBERT.md — Omni-Directional Agent Canon

This is the canonical, model-neutral root file for agents working in this repository.

Chief-only prelude: if the agent is booting as the absolute top orchestrator for Jesse, load `00-TOP-AGENT-PRECOMMAND.md` first, then return here.

Current advanced language note: the live public base is `47D`. The current most advanced public proposal layer is the `49D` hyperglyph overlay documented in `specs/49d-proposal/brown-hilbert-49D-expansion-map.public.json` plus `projections/maps/hyperglyph-map.public.v1.json`. Use `D35` fallback unless those proposal claims are explicitly signed. If the job touches `MiniMax-AI` or `MiniMax-M2.7`, anchor it to `projections/catalogs/minimax.public-catalog.v1.json`, `examples/minimax-m27/MiniMaxM27NamedWave.public.md`, and `examples/minimax-m27/hg256-minimax-catalog-ingest.public.packet.json`.

Rule: keep each Brown-Hilbert cube point at or under 35 lines.

Load only the minimal tracked envelope for the job:

- named agent
- profile
- device
- PID
- timestamp
- task scope

Load order:

0. `00-TOP-AGENT-PRECOMMAND.md` (chief only; skip for non-chief agents)
1. `BROWN-HILBERT.md`
2. `brown-hilbert/01-identity.md`
3. `brown-hilbert/02-continuity.md`
4. `brown-hilbert/03-operating-model.md`
5. `brown-hilbert/04-references.md`
6. Then the relevant dated Brown-Hilbert cube points for the task at hand.
7. Any adapter file that launched you:
   - `AGENTS.md`
   - `CLAUDE.md`
   - `CODEX.md`
   - `ORACLE.md`
   - `ROSE.md`

If the job needs the current most advanced language or glyph semantics, load the public `49D` proposal map and HG256 atlas immediately after this file and before any deeper envelope.

If the job touches `MiniMax-AI` or `MiniMax-M2.7`, also load the public catalog projection, named-wave example, packet placeholder, schema placeholder, hookwall projection, confusable policy, and GC policy before executing any claim-bearing work.

If files disagree:

- Brown-Hilbert root plus Brown-Hilbert cube points win on identity, authority, boot order, and civilization law.
- Adapter files may add runtime-specific launch notes only.
- The agent-first scan doctrine is defined in Brown-Hilbert cube point 03 and governs scan/seek/find behavior.

Private runtime-specific overlays are intentionally omitted from this public root file.

Do not let a model-branded startup file become the only source of truth.
